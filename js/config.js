// --- CONFIG ---

const APP_CONFIG = {
  // Prism.js language class mapping (used for output highlighting)
  prismLanguages: {
    html: 'markup',
    css: 'css',
    javascript: 'javascript',
    php: 'php',
    json: 'json'
  },

  // Default formatting options
  defaults: {
    indentSize: 2,
    preserveNewlines: true,
    wrapLineLength: 120,
    safeMode: true,
    deepMode: false,
    preserveQuotedKeys: true,
    deepStringValues: false
  }
};
